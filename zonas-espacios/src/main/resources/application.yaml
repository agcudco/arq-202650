spring:
  datasource:
    url: jdbc:mysql://localhost:3306/parkin_bd?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: parkin_user
    password: parkin_pass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8080